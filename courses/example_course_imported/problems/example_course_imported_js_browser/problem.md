---
name: ボタンクリックでテキスト変更（JavaScript問題）
timeLimitMs: 10000
---

## 問題文

ページには `id="message"` の `p` 要素と `id="change-btn"` のボタンが配置されています。

ボタンがクリックされたときに、`p` 要素のテキストを `こんにちは！` に変更するJavaScriptを書いてください。
変更後、`test()` 関数を呼び出して結果を確認してください。
